FROM node:20-alpine

WORKDIR /app

# Install json-server globally
RUN npm install -g json-server

# Copy database file
COPY db.json .

EXPOSE 3000

# Start json-server
# Host 0.0.0.0 is important for Docker networking
CMD ["json-server", "--watch", "db.json", "--port", "3000", "--host", "0.0.0.0"]
